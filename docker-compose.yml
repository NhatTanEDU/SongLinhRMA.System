services:
  backend:
    build:
      context: .
      dockerfile: RMA.Server/Dockerfile
    ports:
      - "80:8080"
    volumes:
      - ./RMA.Server/serviceAccountKey.json:/app/serviceAccountKey.json:ro
    restart: unless-stopped
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
